class entry {
    constructor(id, name, length, breadth, height, volume, cft, weight) {
        (this.id = id),
        (this.name = name),
        (this.length = length),
        (this.breadth = breadth),
        (this.height = height),
        (this.volume = volume),
        (this.cft = cft),
        (this.weight = weight);
    }
}

export default entry;