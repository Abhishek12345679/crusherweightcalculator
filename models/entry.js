class entry {
    constructor(name, id, length, breadth, height, volume, cft, weight) {
        (this.id = id),
        (this.length = length),
        (this.breadth = breadth),
        (this.height = height),
        (this.volume = volume),
        (this.cft = cft),
        (this.weight = weight);
    }
}

export default entry;